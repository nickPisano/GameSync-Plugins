name: Validate plugins

on:
  pull_request:
    paths:
      - "plugins/**"
      - "schema/**"
      - "tools/**"
      - ".github/workflows/validate.yml"
  push:
    branches: [main]

jobs:
  validate:
    name: Validate plugins & catalog
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      # No dependencies to install — everything is pure Node.
      - name: Run validator
        run: node tools/validate.js

      - name: Test Elden Ring save parser
        run: node tools/test-er-save-info.js

      - name: Test multi-game Souls save inspector
        run: node tools/test-souls-save-info.js
